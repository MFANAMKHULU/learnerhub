<?php
/*$servername = "localhost";
$username = "root";
$password = "";
$dbname = "katdb"; 


# Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
# Check connection
if ($conn->connect_error) {
  die("Connection failed: " . $conn->connect_error);
} 

# create table
$sql = "CREATE TABLE TechCodeInterns (
 learnerid INT(6) PRIMARY KEY,   
internName VARCHAR(60) NOT NULL,
Surname VARCHAR(60) NOT NULL,
# homeAddress VARCHAR(70) NOT NULL,
empNum INT(10) NOT NULL
)";

if ($conn->query($sql) === TRUE) {
  echo "Table TechCode interns created successfully";
} else {
  echo "Error creating table: " . $conn->error;
}

$conn->close();*/
?>

<?php