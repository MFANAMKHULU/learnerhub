function clockIn() {
    // Add logic to send clock-in request to server (AJAX recommended)
    alert('Clocked In!');
}

function clockOut() {
    // Add logic to send clock-out request to server (AJAX recommended)
    alert('Clocked Out!');
}
